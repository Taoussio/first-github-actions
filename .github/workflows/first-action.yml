name: First Workflow
on: [push, pull_request]

jobs: 
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello World!"
      - name: Print bye
        run: echo "see U -bye!"
